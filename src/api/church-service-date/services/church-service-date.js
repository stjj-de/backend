'use strict';

/**
 * church-service-date service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::church-service-date.church-service-date');
